'use strict';
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
const nasCp = require('../../nas/cp');
const { detectTplPath, getTpl, validateTplName, getBaseDir } = require('../../tpl');
const validate = require('../../validate/validate');
const path = require('path');
const { red } = require('colors');
function cp(srcPath, dstPath, options) {
    return __awaiter(this, void 0, void 0, function* () {
        const tplPath = yield detectTplPath(false);
        if (!tplPath) {
            throw new Error(red('Current folder not a fun project\nThe folder must contains template.[yml|yaml] or faas.[yml|yaml] .'));
        }
        validateTplName(tplPath);
        yield validate(tplPath);
        const tpl = yield getTpl(tplPath);
        const baseDir = getBaseDir(tplPath);
        const recursive = options.recursive || false;
        const noClobber = !options.clobber || false;
        const localNasTmpDir = path.join(baseDir, '.fun', 'tmp', 'nas', 'cp');
        yield nasCp(srcPath, dstPath, recursive, noClobber, localNasTmpDir, tpl, tplPath, baseDir, false);
    });
}
module.exports = cp;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const astNode_1 = require("./astNode");
class PropertyASTNode extends astNode_1.ASTNode {
    constructor(parent, key, start, end, customTag) {
        super(parent, 'property', start, end, customTag);
        this.key = key;
        this.key.parent = this;
        this.slot = key.value;
    }
    getChildNodes() {
        return this.value ? [this.key, this.value] : [this.key];
    }
    getValue() {
        return this.value;
    }
    setValue(value) {
        this.value = value;
        return value != null;
    }
    validate(schema, validationResult, matchingSchemas) {
        if (this.value) {
            this.value.validate(schema, validationResult, matchingSchemas);
        }
    }
}
exports.PropertyASTNode = PropertyASTNode;

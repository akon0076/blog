import { ReactChild } from 'react';
import { RouteComponentProps } from 'react-router-dom';
declare type IProps<T = {}> = T & {
    children: ReactChild;
};
declare type SProps<T = {}> = T & RouteComponentProps;
interface Action {
    type: string;
    payload: object;
}
export { IProps, Action, SProps };
